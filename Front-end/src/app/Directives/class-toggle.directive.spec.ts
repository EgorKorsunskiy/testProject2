import { ClassToggleDirective } from './class-toggle.directive';


describe('TDirective', () => {
  it('should create an instance', () => {
    let directive: ClassToggleDirective
    expect(directive).toBeTruthy();
  });
});
